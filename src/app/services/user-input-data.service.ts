import { Injectable } from '@angular/core';

/** Imports needed for the DB */
import {AngularFirestore, AngularFirestoreCollection, DocumentReference } from '@angular/fire/firestore';

@Injectable({
  providedIn: 'root'
})
export class UserInputDataService {

  constructor() { }
}
